#include "main.h"

/**
 * mul - multiply two integers
 * @a: the first number to multiply
 * @b: the second number to multiply
 * Return: the result between multiplication param a and b
 */
int mul(int a, int b)
{
return (a * b);
}
