#include "main.h"

/**
 * _isupper - check if a number is upper
 * @c: number to be checked
 * Return: return 1 for uppercase, return 0 for else
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
