#include "main.h"

/**
 * _strcat - a function that concatenate two string
 * @dest: parameter
 * @src: parameter
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
int i;
int j;
for (i = 0; src[i] != '\0'; i++)
{
}
for (j = 0; dest[i] != '\0'; j++)
{
}
dest[j] += src[i];
return (dest);
}
